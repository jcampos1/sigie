{% extends '::base2.html.twig' %}
{% block title %}Complejidades{% endblock %}
{% block body -%}
    {% for flashMessage in app.session.flashbag.get('mensaje') %}
        <div class="alert alert-success text-center">
            {{ flashMessage }}
        </div>
    {% endfor %}
    <h1><small>LISTA  DE COMPLEJIDADES</small></h1>
    <div class="text-right">
        <h3><small><em>Cantidad de complejidades: {{ pagination.getTotalItemCount }}</h3></small></em>
    </div>
    <table class="records_list table table-striped">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in pagination %}
            <tr>
                <td><a href="{{ path('complejidad_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombre }}</td>
                <td>
                    <ul class="list-inline">
                        <li>
                            <a href="{{ path('complejidad_show', { 'id': entity.id }) }}">
                                <button type="submit" class="btn btn-info btn-xs"> 
                                    <span class="glyphicon glyphicon-eye-open"></span> Ver
                                </button>
                            </a>
                        </li>
                        <li>
                            <a href="{{ path('complejidad_edit', { 'id': entity.id }) }}">
                                <button type="submit" class="btn btn-primary btn-xs"> 
                                    <span class="glyphicon glyphicon-wrench"></span> Editar
                                </button>
                            </a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <div class="paginacion">
        {{ knp_pagination_render(pagination) }}
    </div>
    <div class="text-right">
        <ul>
            <li>
                <a href="{{ path('complejidad_new') }}">
                    <button type="submit" class="btn btn-primary btn-lg"> 
                        <span class="glyphicon glyphicon-plus-sign"></span> Crear complejidad
                    </button>
                </a>
            </li>
        </ul>
    </div>
    {% endblock %}
